{{#if resume.volunteer}}
<!-- Volunteer Section -->
<section class="volunteer-section mb-5" id="volunteer">
    <h2 class="section-title mb-4">
        <i class="bi bi-heart-fill me-2"></i>
        Volunteer Experience
    </h2>
    
    {{#each resume.volunteer}}
    <div class="experience-item mb-4">
        <div class="row">
            <div class="col-md-8">
                <h4 class="mb-1">{{this.position}}</h4>
                <h5 class="text-primary mb-2">
                    {{#if this.url}}
                    <a href="{{this.url}}" target="_blank" class="text-decoration-none">{{this.organization}}</a>
                    {{else}}
                    {{this.organization}}
                    {{/if}}
                </h5>
            </div>
            <div class="col-md-4 text-md-end">
                <span class="badge bg-success">{{dateRange this.startDate this.endDate}}</span>
            </div>
        </div>
        {{#if this.summary}}
        <p class="mt-2 text-muted">{{this.summary}}</p>
        {{/if}}
        {{#if this.highlights}}
        <ul class="mt-2">
            {{#each this.highlights}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
        {{/if}}
    </div>
    {{/each}}
</section>
{{/if}}